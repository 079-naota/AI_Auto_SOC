# 開発日記  
## 各VPSの現状把握からVPS1のcron開始まで

## 1. 開発目的

本開発では、VPS2・VPS3・VPS4に設置した各ハニーポットから攻撃ログを取得し、30分ごとにVPS1へ集約する仕組みを構築した。さらに、VPS1に集約されたJSONログをGemini APIへ渡し、AIによる攻撃分析レポートを自動生成するところまでを実装した。

構成は以下の通りである。

- VPS1：ログ集約、Gemini解析、レポート生成
- VPS2：Web系ハニーポット
- VPS3：DB系ハニーポット
- VPS4：IoT系ハニーポット

---

# 2. 開発手順

## 2.1 VPS2・VPS3・VPS4の現状確認

最初に、各VPSでDionaeaが正常に稼働しているか、どのポートを公開しているか、ログとSQLiteがどこに保存されているかを確認した。

確認には主に以下を使用した。

```bash
sudo docker ps
find ~/ -name "dionaea.log" 2>/dev/null
sudo docker exec <container_name> find / -name "*.sqlite" 2>/dev/null
sudo ss -tulpn
```

VPS2ではWeb系として80番・443番ポート、VPS3ではMySQL・MSSQL系、VPS4ではIoT向けとして23番・1883番・1900番ポートを使用していることを確認した。

また、Dionaeaのログは主に以下に存在していた。

```text
/home/ubuntu/honeypot/dionaea/logs/dionaea/dionaea.log
```

一方、SQLiteは初期状態ではDockerコンテナ内の、

```text
/opt/dionaea/var/lib/dionaea/dionaea.sqlite
```

に保存されていた。

VPS2では当初、古いSQLiteコピーを参照していたため、新しい攻撃ログとSQLiteの内容が一致しない問題が発生した。【問題1】

---

## 2.2 Dionaea SQLiteの永続化

各VPSでcollectorから最新のSQLiteを参照できるように、DionaeaのSQLiteをホスト側へ永続化した。

docker-compose.ymlに以下を追加した。

```yaml
- ./dionaea/data:/opt/dionaea/var/lib/dionaea
```

その後、コンテナ内に存在していたSQLiteをホスト側へコピーした。

```bash
sudo docker cp \
dionaea-iot:/opt/dionaea/var/lib/dionaea/dionaea.sqlite \
~/honeypot/dionaea/data/
```

所有者も修正した。

```bash
sudo chown -R ubuntu:ubuntu ~/honeypot/dionaea/data
```

Dockerを再作成した後、ホスト側SQLiteから接続件数を取得できることを確認した。

```bash
sqlite3 ~/honeypot/dionaea/data/dionaea.sqlite "
SELECT COUNT(*) FROM connections;
"
```

これにより、collectorはDocker内部の一時的なデータではなく、実際に更新され続けるSQLiteを参照できるようになった。

---

## 2.3 SQLiteの構造確認

collectorを作成する前に、SQLiteの実際のカラム構成を確認した。

```sql
PRAGMA table_info(connections);
```

確認できた主要カラムは以下である。

```text
connection
connection_type
connection_transport
connection_protocol
connection_timestamp
local_host
local_port
remote_host
remote_port
```

送信元IPには `remote_host`、攻撃対象ポートには `local_port` を使用することとした。

当初はSQLiteのカラム名を推測して処理していたため、存在しないカラムを参照する問題が発生した。【問題2】

---

## 2.4 VPS2 Web collectorの確認・修正

VPS2ではDionaeaのHTTPログとSQLiteの接続情報を組み合わせて、Web攻撃を正規化JSONへ変換する処理を構築した。

Dionaeaログには以下のようなHTTPリクエストが記録される。

```text
GET /solr/admin/cores HTTP/1.1
host: ...
user-agent: ...
```

これを解析し、

- HTTPメソッド
- パス
- User-Agent
- Host
- 送信元IP
- 時刻

をJSONとして出力した。

ただし、SQLite内ではWeb通信のプロトコル名が `http` ではなく `httpd` と記録されていたため、当初はWebイベントが取得できなかった。【問題3】

そのため、以下を対象プロトコルとした。

```text
httpd
http
tls
```

---

## 2.5 collectorの差分読み込み化

Dionaeaのログファイルは長期間稼働すると非常に大きくなる。

そこで毎回ログ全体を読み込むのではなく、

```text
state/log_position.json
```

へ前回読み込んだファイル位置を保存する方式へ変更した。

次回実行時には、

```python
f.seek(start)
```

により前回以降に追加された部分だけを読み込む。

これにより30分cronで繰り返し実行しても、過去ログ全体を毎回解析する必要がなくなった。

VPS2では初期実装で `readlines()` によって巨大ログをまとめて読み込んでいたため、処理負荷が高くなる問題があった。【問題4】

---

## 2.6 同一送信元IPの24時間除外処理

同じ攻撃元IPから短時間に大量のアクセスが発生すると、AI解析対象が同じIPで埋まる可能性がある。

そこで、

```text
state/analyzed_ips.json
```

に一度解析した送信元IPと解析時刻を保存した。

同一IPが再度出現した場合でも、

```text
前回解析から24時間未満
```

であれば新しい解析対象から除外するようにした。

これにより、限られたAI解析回数を異なる攻撃元IPへ使用できる構成とした。

---

## 2.7 VPS3 DB collectorの確認

VPS3ではDB系ハニーポットとしてDionaeaを使用し、MySQL・MSSQL系の接続を収集する。

VPS3については、この段階ですでに以下が完成していた。

- SQLiteログ取得
- DB攻撃ログの正規化
- 24時間IP重複除外
- VPS1へのSSH/SCP転送
- `run_collect_send.sh`
- 30分cron

そのため今回の作業では、VPS3は既存設定を維持し、VPS1への送信状態を確認する形とした。

---

## 2.8 VPS4 IoT環境の確認

VPS4では以下のポートを公開していた。

```text
23/tcp
1883/tcp
1900/udp
```

それぞれ主に、

```text
23   : Telnet
1883 : MQTT
1900 : UPnP
```

を対象としている。

SQLiteの接続件数を確認すると、

```text
218831 connections
```

存在しており、VPS2よりもかなり多くのログが蓄積していた。

---

## 2.9 VPS4 collector実行時のメモリ不足対策

VPS2用collectorを基にVPS4用collectorを実行したところ、

```text
Killed
```

とだけ表示され、Pythonが終了した。

`free -h` と `dmesg` を確認した結果、

```text
Out of memory: Killed process ... python3
```

が確認された。

Pythonプロセスは約1.5GBのメモリを使用していた。

VPS4はRAM約2GB、Swapなしであったため、LinuxのOOM Killerによって停止していた。【問題5】

対策として、SQLiteの全件取得をやめ、

```sql
ORDER BY connection_timestamp DESC
LIMIT 10000
```

として最新接続のみ取得するようにした。

また、約1.3GBあったDionaeaログについても、現在の末尾を `log_position.json` に設定し、過去ログ全体を初回から解析しないようにした。

---

## 2.10 VPS4 IoT用collectorへの変更

VPS2用collectorはWeb用であり、

```text
80
443
```

のみを処理するようになっていた。

VPS4では、

```text
23
1883
1900
```

が対象であるため、そのまま流用することはできなかった。【問題6】

VPS4用ではIoTポートを識別し、

```text
23    → TELNET
1883  → MQTT
1900  → UPNP
```

として分類するよう変更した。

---

## 2.11 ログ時刻とSQLite時刻の照合修正

VPS4で以下を実行し、Telnet接続を発生させた。

```bash
nc -vz 127.0.0.1 23
```

SQLiteには、

```text
2026-08-10 00:29:53|172.18.0.1|23|Blackhole
```

と記録された。

Dionaeaログにも、

```text
[10082026 00:29:53]
```

と記録されていた。

しかしcollectorでは、

```text
Collected: 0 events
```

となった。

原因は、Dionaeaログの時刻をJSTとして扱い、さらにUTCへ変換していたことで9時間のずれが生じていたためである。【問題7】

DionaeaログがUTCであることを確認し、

```python
dt.replace(tzinfo=timezone.utc)
```

としてそのままUTC時刻として扱うよう修正した。

修正後は、

```text
Collected: 1 events
```

となった。

---

## 2.12 ログとSQLiteのポート照合

単純に時刻だけでSQLiteの接続情報とログを照合すると、同じ時間帯に複数サービスへの接続が存在した場合、別の接続と誤って対応付ける可能性がある。

そこでDionaeaログの、

```text
[172.18.0.2:23->200.86.206.52:49180]
```

からローカルポートを取得した。

照合条件を、

```text
時刻
+
local_port
```

とすることで、より正確にSQLiteの接続情報と対応付けるようにした。【問題8】

---

## 2.13 VPS4のログ取得確認

修正後、外部からのTelnetアクセスを正常に取得できた。

出力例は以下である。

```json
{
  "honeypot": "VPS4_IOT",
  "event": "iot_attack",
  "analysis": {
    "service": "TELNET",
    "category": "IoT Attack"
  },
  "data": {
    "src_ip": "200.86.206.52",
    "target_port": 23,
    "protocol": "Blackhole"
  }
}
```

これにより、VPS4でもIoT系攻撃ログを正規化JSONへ変換できることを確認した。

---

## 2.14 VPS2・VPS3・VPS4からVPS1への公開鍵認証

各VPSからVPS1へ自動転送するため、SSH公開鍵認証を使用した。

VPS1の接続先は、

```text
100.99.83.113
```

SSHポートは、

```text
2222
```

である。

各VPSで鍵を生成し、

```bash
ssh-keygen -t ed25519
```

VPS1へ公開鍵を登録した。

```bash
ssh-copy-id -p 2222 ubuntu@100.99.83.113
```

これによりcronからパスワード入力なしでSCP転送できるようになった。

---

## 2.15 各VPSからVPS1へのJSON転送

転送先は以下とした。

```text
VPS2
/home/ubuntu/Auto_soc/data/honeypot_logs/vps2_web.json

VPS3
/home/ubuntu/Auto_soc/data/honeypot_logs/vps3_db.json

VPS4
/home/ubuntu/Auto_soc/data/honeypot_logs/vps4_iot.json
```

各VPSでは、

```text
run_collect_send.sh
```

を使用し、

```text
collector実行
↓
JSON生成
↓
SCP転送
```

を一括実行するようにした。

---

## 2.16 各VPSのcron設定

VPS2・VPS3・VPS4では以下を設定した。

```cron
*/30 * * * * /home/ubuntu/collector/run_collect_send.sh
```

これにより毎時、

```text
00分
30分
```

にログ収集とVPS1への送信が行われる。

各VPSで、

```bash
systemctl status cron
```

を確認し、cronが `active (running)` であることを確認した。

---

## 2.17 VPS1でのログ受信確認

VPS1では以下3ファイルが存在することを確認した。

```text
vps2_web.json
vps3_db.json
vps4_iot.json
```

更新時刻もほぼ同時刻となっており、各VPSのcron送信が機能していることを確認した。

また、

```bash
python3 -m json.tool
```

によってJSON構文も確認した。

確認時のイベント数は、

```text
VPS2 Web : 1件
VPS3 DB  : 1件
VPS4 IoT : 2件
```

であった。

---

## 2.18 VPS1でGemini解析確認

VPS1では `.env` に設定した、

```text
GEMINI_API_KEY
```

を使用した。

APIキーが読み込まれていることを確認した後、

```bash
python3 analyze_alerts.py
```

を実行した。

結果、

```text
Generated: report_20260810_1019.md
```

となり、AI解析レポートが正常に生成された。

---

## 2.19 VPS1の自動解析スクリプト作成

cronから解析処理を呼び出せるよう、

```text
/home/ubuntu/Auto_soc/run_analysis.sh
```

を作成した。

処理は、

```text
.env読み込み
↓
analyze_alerts.py実行
↓
Gemini APIへ送信
↓
Reports/30minへレポート生成
↓
analysis_cron.logへ結果記録
```

という流れである。

手動実行したところ、

```text
Generated: report_20260810_1030.md
Analysis completed
```

となり正常に実行できた。

---

## 2.20 VPS1 cronの設定

VPS1には以下を登録した。

```cron
5,35 * * * * /home/ubuntu/Auto_soc/run_analysis.sh
```

VPS2・VPS3・VPS4は00分・30分に送信するため、VPS1では5分遅らせて05分・35分に解析を開始する構成とした。

これにより、

```text
10:00 各VPSからVPS1へJSON転送
10:05 VPS1でGemini解析

10:30 各VPSからVPS1へJSON転送
10:35 VPS1でGemini解析
```

となる。

---

## 2.21 VPS1 cronサービス停止への対応

cron設定後に、

```bash
systemctl status cron
```

を確認すると、

```text
Active: inactive (dead)
```

となっていた。

crontabへ設定を書き込んでいても、cronサービス自体が停止しているため自動解析は実行されない状態であった。【問題9】

以下でcronを起動した。

```bash
sudo systemctl start cron
sudo systemctl enable cron
```

その後、実際に、

```text
===== Mon Aug 10 11:05:01 JST 2026 =====
Generated: report_20260810_1105.md
Analysis completed
```

が記録された。

これによりVPS1のcronからGemini解析とAIレポート生成まで正常に自動実行できることを確認した。

---

# 3. 発生した問題

## 問題1：VPS2で古いSQLiteを参照していた

### 内容

collectorが、

```text
/tmp/dionaea.sqlite
```

を参照していたが、このDBは過去にコンテナからコピーされたものであり、最新ログが反映されていなかった。

Dionaeaログには新しい攻撃が存在する一方、SQLiteには該当接続が存在しないため、送信元IPを取得できなかった。

### 対応

Docker volumeを使用し、

```text
/home/ubuntu/honeypot/dionaea/data/dionaea.sqlite
```

をDionaeaが直接使用する構成に変更した。

---

## 問題2：SQLiteのカラム名を誤って認識していた

### 内容

collector側でSQLiteの構造を推測していたため、実際には存在しないカラムを使用するSQLが発生した。

### 対応

```sql
PRAGMA table_info(connections);
```

で実際のスキーマを確認した。

送信元IPには、

```text
remote_host
```

を使用することを確認した。

---

## 問題3：Web攻撃が0件になる

### 内容

SQLiteの `connection_protocol` を `http` と想定していたが、実際のWeb接続は、

```text
httpd
```

として記録されていた。

そのためcollectorの条件で除外されていた。

### 対応

対象プロトコルに、

```text
httpd
```

を追加した。

---

## 問題4：巨大ログの全読み込み

### 内容

Dionaeaログ全体を `readlines()` で取得していたため、ログが巨大になるにつれて処理時間とメモリ使用量が増加した。

### 対応

前回の読み込み位置を、

```text
state/log_position.json
```

へ保存し、次回以降は差分だけを読む方式に変更した。

---

## 問題5：VPS4 collectorが `Killed` となる

### 内容

VPS4でcollectorを実行したところ、

```text
Killed
```

となった。

`dmesg` には、

```text
Out of memory: Killed process ... python3
```

が記録されていた。

Pythonが約1.5GB使用しており、約2GB RAM・SwapなしのVPS4でOOMが発生していた。

主な要因は、

- Dionaeaログ約1.3GB
- SQLite約21万接続
- SQLite全件取得
- ログ大量読み込み

であった。

### 対応

SQLiteを、

```sql
LIMIT 10000
```

に制限した。

さらにログ末尾から処理を開始し、過去1.3GBを初回から解析しないようにした。

---

## 問題6：Web用collectorをIoT環境へ流用していた

### 内容

VPS2用collectorは、

```text
80
443
```

のみを対象としていた。

一方VPS4では、

```text
23
1883
1900
```

を使用する。

そのためVPS2用条件のままでは、VPS4の接続がすべて除外された。

### 対応

VPS4用に、

```text
TELNET
MQTT
UPNP
```

の分類処理を追加した。

---

## 問題7：DionaeaログとSQLiteに9時間の時刻ずれが発生した

### 内容

Dionaeaログの時刻をJSTとして処理した後UTCへ変換していた。

しかしDionaeaログ自体がUTCで記録されていた。

そのためSQLiteとの間に9時間のずれが発生し、120秒以内という照合条件に一致しなかった。

### 対応

Dionaeaログを最初からUTCとして処理した。

修正後、

```text
Collected: 1 events
```

となり正常に対応付けられた。

---

## 問題8：時刻だけの照合では別接続を取得する可能性があった

### 内容

同じ時間帯に複数のサービスへ攻撃が発生した場合、時刻だけでSQLiteを検索すると別ポートの接続を選択する可能性がある。

### 対応

Dionaeaログから `local_port` を取得し、

```text
時刻 + local_port
```

でSQLiteと照合する方式へ変更した。

---

## 問題9：VPS1のcronが停止していた

### 内容

VPS1へcrontabを登録した後、

```text
Active: inactive (dead)
```

となっていることが判明した。

crontabの設定自体は正しかったが、cronサービスが停止していたため自動実行されない状態だった。

### 対応

```bash
sudo systemctl start cron
sudo systemctl enable cron
```

を実行した。

その後、

```text
11:05:01
Generated: report_20260810_1105.md
Analysis completed
```

が確認され、cronによるAI解析が正常に実行された。

---

# 4. 開発終了時の状態

本開発終了時点では、

```text
VPS2 Web
VPS3 DB
VPS4 IoT
```

の各ハニーポットで攻撃ログを取得し、30分ごとにVPS1へJSON形式で送信する構成が完成した。

VPS1では、各VPSからのログ受信後5分待ってGemini解析を行う。

最終的な処理は以下である。

```text
VPS2 / VPS3 / VPS4
        ↓
攻撃ログ取得
        ↓
正規化JSON生成
        ↓
同一IP24時間除外
        ↓
00分・30分
VPS1へSCP転送
        ↓
05分・35分
Gemini API解析
        ↓
AI解析レポート生成
```

この状態から24時間連続運転を行い、各ハニーポットのJSONログとAI解析レポートを蓄積する段階へ移行する。
